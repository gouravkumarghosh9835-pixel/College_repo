Problem One

Find the maximum sum of a substring such that all characters are unique.
The substring can be cyclic (wrap-around allowed).

Key idea

Use cyclic trick.
Expand substring until a duplicate appears
Calculate sum using(using ASCII values...)



Problem Two 

program calculates the minimum number of operations required to make all elements in an array equal.
Each operation allows increasing or decreasing an element by a fixed value k.


Key Idea

All elements must reach the same value (median), and the difference must be divisible by k.